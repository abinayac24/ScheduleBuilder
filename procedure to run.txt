1) open folder in vs code without onedrive folder ( better in c disk )

2) python -m venv venv

3) .\venv\Scripts\Activate.ps1

4) pip install -r requirements.txt

5) pip install openpyxl 

6) streamlit run website.py  
